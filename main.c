#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "keygen.h"

int main() {
    // this will set random seed
    srand(time(NULL));

    // determine the le
    int key_length = 64;      

}